# Marks the "migrations" folder as a Python package.
# Django stores auto-generated migration files here whenever you
# run "python manage.py makemigrations". Each migration file is a
# step-by-step record of changes made to models.py over time,
# which Django uses to keep the actual database schema in sync
# with our Python model definitions.
