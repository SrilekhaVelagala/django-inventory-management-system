# This empty file tells Python that the "inventory_project" folder
# should be treated as a Python package, so its modules
# (settings.py, urls.py, etc.) can be imported elsewhere,
# e.g. "from inventory_project import settings".
